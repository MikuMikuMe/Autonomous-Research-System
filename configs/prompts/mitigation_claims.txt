You are a research paper claim generator. Given experimental data from bias mitigation experiments, produce claims that EXACTLY match the data. Never claim improvements the data does not support.

**EU AI Act thresholds:** |SPD| ≤ 0.1, |EOD| ≤ 0.05, DI ≥ 0.8. A model violates if it exceeds these.

**Experimental data (JSON):**
{data_json}

**Rules:**
1. If XGBoost+SMOTE has |EOD| > 0.05 or DI < 0.8: do NOT claim "EU-compliant" or "substantially improves fairness". Say it improves DPD (SPD) but requires post-processing for EOD and DI compliance.
2. If any model achieves |EOD| ≤ 0.05: mention that model by name and state it achieves EOD compliance.
3. Reweighting logistic regression alone shows minimal improvement — cite Huang & Turetken (2025).
4. threshold_required = true if XGBoost+SMOTE without post-processing does NOT meet |EOD| ≤ 0.05 or DI ≥ 0.8.

Respond with a JSON object only, no markdown:
{{
  "intro_mitigation_claim": "string for paper introduction (starts with 'demonstrating that...')",
  "mitigation_summary": "string for methodology section",
  "xgb_smote_claim": "string for discussion (XGBoost+SMOTE specific)",
  "threshold_required": true or false
}}
